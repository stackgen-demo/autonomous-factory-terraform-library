terraform {
  backend "s3" {
    bucket       = "__TERRAFORM_STATE_BUCKET__"
    key          = "__TERRAFORM_STATE_KEY__"
    region       = "__AWS_REGION__"
    use_lockfile = true
    encrypt      = true
  }
}
